# PM Stack 软件更新日志

## V1.10.3 — 市场规划字段提取自动补抓 + 解耦流水线

> 发布日期：2026-09-21
> 形态：正式版本发布，兼容原有工作流

### 新增内容

- 「市场规划 → 字段提取」新增「自动补抓缺失的五点描述与主副图」能力，默认开启；输入表已备好数据时可手动取消勾选跳过抓取。
  - 当输入表缺少或留空「五点描述」/图片列时，按「商品详情页链接」列实时抓取补齐后再进入提取流程（边抓边提取）。
  - 每个并发线程独占一个无头 Microsoft Edge，不弹窗；抓取结果按 ASIN 缓存断点，重跑不重复抓。
  - 抓到的五点描述与 `image_1..9` 一并写入输出 Excel。
- 评论采集面板支持导出采集结果，并可一键发送到「评论分析」流程，打通采集与分析。

### 工程与性能

- 将「抓取 + 提取」由耦合式改为解耦式两阶段流水线：
  - 独立 FetchPool 抓取线程池负责抓取，预抓缓冲（prefetch lookahead，默认按抓取线程数自适应）提前把后续 ASIN 的抓取结果备好；
  - 提取 worker 不再阻塞等待自己那一行的抓取，抓取与提取真正并行，总耗时 ≈ N × max(抓取耗时/抓取线程数, 提取耗时/提取并发数)；提取（AI 调用）通常是瓶颈上限。
  - ASIN 级在途去重：预抓与 worker 共享同一个 Future，避免重复抓取。
- 处理 Playwright 线程亲和性：会话必须在创建它的同一线程上关闭，FetchPool 为每个抓取线程持有其整个生命周期的会话。
- 协作式停止：停止时不再提交新抓取任务，快速排空在途请求并保留已取得的结果。

### 验证结果

- 解耦流水线端到端验证：6 行样本、提取并发 2 / 抓取线程 2 / 预抓 6，22 秒完成，退出码 0，无跨线程告警、无 API 重试，输出列序与抓取/提取值均正确。
- 打包路径验证：playwright、`core.reviews`、`scripts/` 均已随包，冻结态脚本目录可正确解析；最终 EXE 约 130.2 MB。

---

## V1.10.2 补充更新 — 评论采集与云端存储集成

> 更新日期：2026-09-10  
> 版本说明：本次为 V1.10.2 功能补充，不变更现有程序版本号

### 新增内容

- 在「评论分析」中新增「评论采集与云端」子模块，与原有 AI 标签提取、汇总洞察、一键完成和批量分析流程并列使用。
- 将原 ReviewCollector 的采集能力迁入 PM Stack，支持基于 Playwright 与 Microsoft Edge 的评论采集流程。
- 新增本地 SQLite 评论库，用于任务、评论、采集状态和失败重试数据的持久化。
- 新增云端同步队列（Outbox），支持将本地评论可靠同步到 Supabase，并保留失败记录以便重试。
- 新增评论导出适配：统一输出 PM Stack 可直接分析的 Excel 数据，并兼容按星级拆分工作表。
- 支持显式导入旧 ReviewCollector 数据库；导入过程不会修改或删除原数据库。

### 界面与交互

- 评论分析页面增加三级子标签，用于区分评论分析、评论采集、本地数据和云端同步相关操作。
- 采集任务支持运行状态、日志、进度和错误信息展示。
- 增加协作式取消机制，用户停止任务时可安全结束采集并保留已写入数据。

### 工程与打包

- 评论采集后端统一迁入 `core/reviews`，减少与原独立项目的耦合。
- 补充 Playwright、SQLite、Supabase 同步及 Excel 导出所需依赖和打包资源。
- 处理 PyInstaller 打包中的 ICU 动态库冲突，确保采集浏览器与主程序可共同运行。
- 使用 Python 3.11 独立虚拟环境完成构建，规避旧 Python 3.9 环境的 TLS 兼容问题。

### 验证结果

- 评论模块自动化测试：45 项通过。
- 已验证 Microsoft Edge 启动、SQLite 读写、旧数据库导入、Excel 导出和云端队列流程。
- 已完成打包程序启动自检；最终可执行文件约 131.1 MB。
- 原有评论分析工作流保持兼容，源 ReviewCollector 项目未被修改。

---

## V1.10.2 — 评论分析并发测试子模块

> 发布日期：2026-08-26
> 形态：独立内置子模块，与原“评论分析”并行存在

### 背景

评论分析流程中「AI特征提取」采用逐条串行调用 API，单文件处理耗时过长（如 500 条评论约 60 分钟）。本版本在不改动原有代码的前提下，新增并发版测试模块用于验证加速效果。

### 新增内容

- 新增 `scripts/comments_step_1_fast.py`（并发版特征提取）：
  - 与原版 `comments_step_1.py` 保持相同的输入输出格式、prompt 与字段逻辑；
  - 采用线程池并发调用 AI（默认并发 8，界面可调 1~12）；
  - 增加全局 RPM/TPM 滑动窗口限速（默认 900/90,000，可在界面调整）；
  - 429 优先遵循服务端 `Retry-After`，无该响应头时使用递增退避，并加入随机抖动防止并发线程同时重试；
  - 增加单次请求超时，避免停止流程被异常长请求无限阻塞；
  - 结果严格按原行序写回，输出仅末尾追加 `_处理状态` 列标记失败/停止条目，Step 2 汇总脚本完全兼容；
  - 每 20 条原子保存 JSON 断点，重启后跳过已成功行，仅补跑失败或未完成行；
  - 结束时打印真实 HTTP 请求数、重试、429、Token、平均延迟、吞吐和加速比；
  - 采用有界任务窗口，停止时不再提交新任务，并保存在途请求已经取得的结果；
  - Excel 先写同目录临时文件，完整关闭后再原子替换正式输出，避免中途异常损坏旧文件。
- 新增 `modules/comment_analysis_test.py`（侧边栏新模块「评论分析TEST」）：
  - 单卡片"一键完成_TEST"：输入文件 / 品类 / 数据表 / 并发数 / RPM / TPM + 开始、继续补跑、停止；
  - Step 1 只要仍有失败条目就返回专用退出码 2，阻止不完整数据自动进入 Step 2；
  - Step 2 复用原 `comments_step_2.py`，并自动排除 `_处理状态` 中的失败/停止行；
  - 输出文件带 `_TEST` 后缀（`_AI处理_TEST` / `_AI全流程_TEST`），不覆盖原流程结果。

### 兼容性说明

- 原「评论分析」模块与 `comments_step_1.py` 零改动；`comments_step_2.py` 仅增加状态行过滤和 `API_KEY` 兼容回退；
- 新模块按 `order=1.5` 排在原评论分析之后，并随 V1.10.2 主程序 EXE 一并分发。

### 实测数据

- 测试环境：SiliconFlow API（RPM 1000 / TPM 100,000），模型 `deepseek-ai/DeepSeek-V3`，真实评论数据 8 条，并发 4；
- 结果：8 次 API 请求，平均单次 4.0s，实际总耗时 9s（串行估算 31s），加速比约 3.3 倍；
- 结论：并发路径效率接近理论上限，60 分钟级任务预计可压缩至 8~10 分钟。
- 加固后离线回归：19 项测试全部通过，其中 6 项覆盖并发行序、停止保存、429 重试计数、失败门槛、断点补跑和 Step 2 状态过滤。
- 126 条真实评论四档压测（RPM 900 / TPM 90,000）：并发 1/4/8/12 分别耗时 826.26s / 227.95s / 94.45s / 80.14s，四组均为 0 失败、0 重试、0 次 429；相对串行分别加速 3.62 / 8.75 / 10.31 倍。
- 四份输出均为 126/126 行、原评论顺序一致、配置字段齐全；并发 4/8/12 与串行输出的字段级精确一致率分别为 78.46% / 78.80% / 79.02%，差异未随并发升高而扩大，符合温度 0.2 下的模型随机性特征。
- 结论：默认并发调整为 8；并发 12 只比 8 再快约 15 秒且更接近 TPM 上限，保留为高级选项。
- 运行时说明：本机 Python 3.9.6 / OpenSSL 1.1.1k 与 SiliconFlow TLS 握手失败；上述真实压测使用 Python 3.12.13 / OpenSSL 3.5.7 完成。重新打包前必须用目标 Python 运行 `tools/check_llm_connection.py` 验证连接。

### 后续建议

1. **继续稳定性验证**：再连续运行 3~5 个不同品类、不同评论量的真实文件，关注失败补跑、Step 2 全流程和长时间运行稳定性。
2. **推广到原模块**：TEST 版验证稳定后（建议连续跑 3~5 个文件无失败、无格式问题），可将并发逻辑合入原 `comments_step_1.py` 与「评论分析」模块，并移除 TEST 模块，避免长期维护两套流程。
3. **去重缓存**：相同评论文本（如 "Great product!" 类高重复短评）可直接复用结果，降低调用量；上线前需先统计真实重复率。
4. **Step 2 提速评估**：`comments_step_2.py` 仍是串行调用（含 NORMALIZE/CLASSIFY/SELF_CHECK 多轮请求），如端到端统计显示其成为主要瓶颈，再单独设计批处理并发。
5. **模型降档测试**：翻译+标签提取任务可尝试更小更快的模型（如 Qwen 系列轻量版）对比质量与速度。
6. **正式模块合入门槛**：V1.10.2 继续保留独立 TEST 子模块；连续 3~5 个文件验证通过后，再评估是否替换原串行模块。

### 分发通道（2026-08-31 补充）

- 源代码推送至 GitHub 公开仓库：https://github.com/Song-wenpeng/PM-Stack
- 创建 v1.10.2 Release，用于分发 EXE 和完整分发包
- `update-channel.json` 配置为 GitHub Releases 地址，客户端启动时自动检查更新：
  ```
  https://github.com/Song-wenpeng/PM-Stack/releases/latest/download/update.json
  ```
- 新增 `publish.py` 一键发布脚本和 `发布新版本.bat` 启动器，自动化：打包 → 生成 update.json → 推送 → 创建 Release

---

## V1.10.0 — 主程序自动更新

- 新增公司共享盘、`file://` 与公开 HTTPS（含 GitHub Releases）更新通道。
- 使用 `update.json` 发布版本、下载地址、大小、SHA-256、更新说明与强制标记。
- 主程序在后台检查和下载，下载完成后必须通过大小与 SHA-256 双重校验。
- 新增独立的 `PM Stack Updater.exe`，主程序退出后再替换稳定文件名 `PM Stack.exe`。
- 更新前保留 `PM Stack.previous.exe`；替换或启动失败时自动回滚。
- 主程序更新与原插件 ZIP 更新在设置中分开显示，降低误操作风险。
- 新增更新地址验证入口、发布清单生成工具和 5 项自动更新回归测试。

---

## V1.9.1 — 安全热修

- API Key 迁移到 `%LOCALAPPDATA%\PM Stack\config.json`，使用 Windows DPAPI 加密。
- 每个功能模块使用独立 `ScriptRunner` 信号，内嵌脚本仍由全局锁串行执行。
- 插件更新拒绝路径穿越、符号链接和超限 ZIP，并通过暂存目录安全合并。
- 顶部状态调整为“AI 已配置（未验证）”，避免把 Key 存在误报为服务正常。
- Excel 数据修正改为保留工作簿结构的原子保存，并自动生成时间戳备份。
- 修复品类下拉框、自动输出路径、动态筛选条件、排序方式和任务按钮的交互状态。
- 趋势模式会明确标记对应必填 Sheet，重复更新检查会被阻止。
- 新增安全、数据完整性与 UI 交互回归测试。

---

## V1.2 — 插件化架构更新

## 版本信息

| 项目 | 内容 |
|---|---|
| 版本号 | v1.2.0 |
| 发布日期 | 2026-06-23 |
| 前身 | 电商数据处理工具集 |
| 定位 | 可迭代插件化工具平台 |

---

## 一、架构升级：插件化

### 1.1 外部插件目录

新增 `plugins/` 目录，位于 EXE 同级，用户可自由增删插件而无需重新打包。

```
PM Stack V1.2.exe 所在目录/
├── PM Stack V1.2.exe
├── config.json
└── plugins/
    ├── modules/          ← 放插件模块（GUI 面板）
    │   └── my_tool.py
    └── scripts/          ← 放插件脚本（分析脚本）
        └── my_script.py
```

**插件编写约定**（与内置模块完全一致）：
- 暴露 `MODULE_INFO` dict：`{"name", "icon", "order", "description"}`
- 暴露 `ModuleWidget(QWidget)` 类，构造函数签名 `(config_mgr, runner)`
- 放入 `plugins/` 目录后重启即生效

### 1.2 插件脚本覆盖

`plugins/scripts/` 中的同名脚本优先于内置脚本。例如：
- 内置 `scripts/sales_number.py`
- 插件 `plugins/scripts/sales_number.py`（覆盖内置版本）

查找顺序：`plugins/scripts/` → 内置 `scripts/`

### 1.3 模块加载优先级

启动时按以下顺序扫描模块：
1. 内置 `modules/`（打包在 EXE 内部）
2. 外插 `plugins/modules/`（EXE 同级目录）
3. 按 `MODULE_INFO["order"]` 排序
4. 同名模块内置优先（防止插件覆盖核心功能）

---

## 二、架构升级：GitHub 自动更新

### 2.1 工作原理

```
APP 启动
  ↓
读取 config.json 中的 github_repo 和 auto_check_update
  ↓
GET https://api.github.com/repos/{owner}/{repo}/releases/latest
  ↓
比较 tag_name 与本地 current_version
  ↓
有更新 → 弹窗提示 → 用户确认 → 下载 zip → 解压到 plugins/ → 提示重启
无更新 → 静默跳过，不阻塞启动
网络异常 → 静默跳过
```

### 2.2 配置方式

在 **设置 → 更新与插件** 面板中：

| 配置项 | 说明 | 示例 |
|---|---|---|
| GitHub 仓库 | `用户名/仓库名` | `myname/pm-stack-modules` |
| 启动时自动检查更新 | 开关 | 默认开启 |
| 立即检查更新 | 手动触发按钮 | — |

### 2.3 插件仓库结构

在 GitHub 创建公开仓库，结构如下：

```
repo: username/pm-stack-modules
├── README.md
└── releases/
    └── v1.2.1/              ← tag 名即版本号
        └── plugins.zip      ← release asset
```

`plugins.zip` 内部结构：
```
plugins.zip
├── modules/
│   └── new_module.py
└── scripts/
    └── new_script.py
```

### 2.4 版本比较规则

- 格式：`vX.Y.Z`（如 `v1.2.0`）
- 逐段比较：`v1.2.1 > v1.2.0`，`v1.3.0 > v1.2.9`
- 无需认证（公开仓库），API 限额 60 次/小时

---

## 三、设置面板扩展

新增 **"更新与插件"** 分组：

```
┌─────────────────────────────────────┐
│ 更新与插件                           │
│                                     │
│ GitHub 仓库:  [username/repo      ] │
│ ☑ 启动时自动检查更新                  │
│ [立即检查更新]  发现新版本 v1.2.1     │
└─────────────────────────────────────┘
```

---

## 四、状态栏改进

| 项目 | 电商数据工具集 | PM Stack V1.2 |
|---|---|---|
| 显示内容 | `就绪 \| API: 已配置 \| 版本: 1.0.0` | `就绪 \| API: 已配置 \| 模块: 4 \| 版本: v1.2.0` |
| 模块计数 | 无 | 显示已加载模块总数 |

---

## 五、配置文件扩展

`config.json` 新增字段：

```json
{
  "github_repo": "",
  "auto_check_update": true,
  "current_version": "v1.2.0"
}
```

---

## 六、文件结构对比

```
电商数据工具集/                PM Stack V1.2/
├── main.py                   ├── main.py
├── config.json               ├── config.json          ← 新增更新配置
├── core/                     ├── core/
│   ├── main_window.py        │   ├── main_window.py   ← 插件扫描 + 更新入口
│   ├── config_manager.py     │   ├── config_manager.py ← 插件目录 + resolve_script
│   ├── runner.py             │   ├── runner.py
│   ├── codex_theme.py        │   ├── codex_theme.py
│   └── base_module.py        │   ├── base_module.py
│                             │   └── updater.py       ★ 新增：GitHub 更新器
├── modules/ (4个)            ├── modules/ (4个)
├── scripts/                  ├── scripts/
│                             ├── plugins/              ★ 新增：外部插件目录
│                             │   ├── modules/
│                             │   └── scripts/
└── 电商数据工具集.spec         └── PM Stack V1.2.spec
```

---

## 七、使用指南

### 首次使用

1. 运行 `PM Stack V1.2.exe`
2. 进入 **设置**，配置 API Key
3. 如需自动更新，填写 GitHub 仓库地址

### 安装插件

1. 将插件 `.py` 文件放入 `plugins/modules/`
2. 如插件附带脚本，放入 `plugins/scripts/`
3. 重启软件，新模块自动出现在左侧导航

### 发布更新（开发者）

1. 编写插件 `.py` 文件
2. 打包为 `plugins.zip`（内含 `modules/` 和/或 `scripts/` 子目录）
3. 在 GitHub 仓库创建 Release，tag 如 `v1.2.1`，上传 zip 作为 asset
4. 用户启动软件时自动收到更新提示

### 手动检查更新

1. 进入 **设置 → 更新与插件**
2. 确认 GitHub 仓库地址已填写
3. 点击 **立即检查更新**

---

## 八、兼容性

| 项目 | 说明 |
|---|---|
| 内置模块 | 与电商数据工具集完全一致，无功能损失 |
| 脚本兼容 | 所有内置脚本原样继承，环境变量接口不变 |
| 配置兼容 | 原 config.json 字段全部保留，新增字段有默认值 |
| EXE 模式 | 完全支持，插件目录在 EXE 同级 |
| 源码模式 | 完全支持，插件目录在 PM Stack/ 同级 |
