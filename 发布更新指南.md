# PM Stack 主程序发布与更新指南

适用于小范围同事使用。V1.10.0 起，客户端固定使用两个文件名：

```text
PM Stack.exe
PM Stack Updater.exe
```

从旧的 `PM Stack V1.9.1.exe` 升级到 V1.10.0 需要手动分发一次；之后可由软件自动更新。

## 一、首次分发

运行 `发布新版本.bat` 后，脚本会自动生成干净的 `release/current/`
分发目录和 `release/archive/vX.Y.Z/` 历史归档。把 `release/current/`
中的以下文件复制到每位同事的本地文件夹，并保持同级：

```text
PM Stack.exe
PM Stack Updater.exe
update-channel.json
```

不要让同事直接在共享盘运行 EXE。共享盘只作为下载源，程序应复制到本机再运行，避免多人占用、断网和权限问题。

`dist/` 仅作为构建目录，不用于保存历史 EXE、用户配置、快捷方式或运行生成的图表。
历史版本统一保存在 `release/archive/`；每次发布还会在对应版本归档中生成仅包含
上述三个文件的 `PM Stack vX.Y.Z 首次分发.zip`。

在 `update-channel.json` 中设置统一的清单地址：

```json
{
  "manifest_source": "\\\\server\\software\\PM Stack\\update.json"
}
```

也可以不修改该文件，让同事在“设置 → 主程序更新”填写地址。设置值会保存在当前 Windows 用户配置中，并优先于 `update-channel.json`。

## 二、公司共享盘发布

1. 建立只允许维护者写入、普通同事只读的目录，例如 `\\server\software\PM Stack`。
2. 将新版本 `PM Stack.exe` 复制到该目录。
3. 最后生成并复制 `update.json`。清单一定要最后更新，避免客户端先读到清单却下载到旧文件。

生成命令：

```powershell
python tools\make_update_manifest.py `
  --exe "dist\PM Stack.exe" `
  --version "v1.10.2" `
  --url "PM Stack.exe" `
  --notes "本次更新说明" `
  --output "update.json"
```

`url` 使用相对文件名时，程序会从 `update.json` 所在目录下载 EXE。

## 三、公开 GitHub Releases 发布

1. 创建公开 GitHub 仓库并新建 Release，标签使用 `vX.Y.Z`。
2. 上传 `PM Stack.exe` 和生成好的 `update.json` 两个 Release assets。
3. 客户端清单地址使用：

```text
https://github.com/OWNER/REPO/releases/latest/download/update.json
```

清单里的 `url` 保持为 `PM Stack.exe` 即可；客户端会解析为同一 Release 下载目录。文件名有空格时程序会自动进行 URL 编码。

### 3.1 发布前检查

发布前不要只确认 `api.github.com` 或 `git push` 可用。GitHub Release 创建与附件上传使用不同端点：

- Release 创建、查询：`api.github.com`
- Release 附件上传：`uploads.github.com`
- Git 推送：`github.com`

三者应分别验证。某一端点直连成功，不代表附件上传链路可用。

```powershell
# 检查 gh 登录；若显示 token invalid，先重新授权
gh auth status

# 检查代理端口（以下端口按本机实际配置替换）
Test-NetConnection 127.0.0.1 -Port 7897

# 核对待发布程序的大小与哈希
Get-Item "dist\PM Stack.exe" | Select-Object Name, Length
Get-FileHash -Algorithm SHA256 "dist\PM Stack.exe"
```

同时确认以下内容完全一致：

1. `update.json.version`、Release tag 和程序内版本号；
2. `update.json.size`、`sha256` 与实际 EXE；
3. `update.json.url` 末尾文件名与 Release 附件名，包括空格、连字符和大小写；
4. Release 标记为 latest，且不是 draft/prerelease；
5. `update-channel.json` 指向 `releases/latest/download/update.json`。

### 3.2 受限网络下通过代理发布

如果 `uploads.github.com:443` 直连超时，但本机代理可访问，应仅为当前进程设置代理，避免无意改变全局 Git 配置：

```powershell
$env:HTTPS_PROXY = "http://127.0.0.1:7897"
$env:HTTP_PROXY = "http://127.0.0.1:7897"

gh auth status
gh release create vX.Y.Z --repo OWNER/REPO `
  --title "PM Stack vX.Y.Z" --notes "本次更新说明" --latest

gh release upload vX.Y.Z --repo OWNER/REPO `
  "待上传\PM-Stack.exe" `
  "待上传\PM-Stack-vX.Y.Z.zip" `
  "待上传\update.json"
```

130 MB 以上附件建议采用“先创建空 Release，再逐个上传”的方式。这样即使大文件上传失败，也可以直接重试 `gh release upload`，不必反复创建和删除 Release。

若 `gh auth status` 显示登录令牌失效，通过相同代理重新授权：

```powershell
$env:HTTPS_PROXY = "http://127.0.0.1:7897"
$env:HTTP_PROXY = "http://127.0.0.1:7897"
gh auth login --hostname github.com --git-protocol https --web
```

一次性授权码和登录凭据不得写入仓库、日志或问题文档。

### 3.3 失败恢复与发布后验证

- 创建 Release 失败：先查询远端是否已经留下 tag 或 draft，禁止未经检查就重复创建。
- 只有部分附件上传成功：保留 Release，使用 `gh release upload --clobber` 补传或替换对应附件。
- 确认 Release 和 tag 都无有效内容时，才使用 `gh release delete vX.Y.Z --cleanup-tag` 回滚。
- 不要在确认远端发布成功前，把“正式版本发布”作为唯一完成证据；本地 EXE、Git 提交和 GitHub Release 是三个独立状态。

发布后至少执行以下只读验证：

```powershell
gh release view vX.Y.Z --repo OWNER/REPO `
  --json tagName,isDraft,isPrerelease,publishedAt,assets,url
```

并分别访问：

```text
https://github.com/OWNER/REPO/releases/latest/download/update.json
https://github.com/OWNER/REPO/releases/download/vX.Y.Z/PM-Stack.exe
```

下载后的 `update.json` 必须再次核对版本、附件 URL、大小和 SHA-256。最后用一台旧版本客户端完成一次真实更新，确认下载、校验、替换、重启和回滚副本都正常，才将发布标记为完成。

## 四、客户端更新过程

1. 启动时后台读取 `update.json`，比较 `vX.Y.Z` 版本号。
2. 用户确认后下载主程序，并校验清单中的文件大小和 SHA-256。
3. `PM Stack Updater.exe` 等待主程序退出。
4. 当前版本改名为 `PM Stack.previous.exe`，新版本替换为 `PM Stack.exe`。
5. 再次校验通过后启动新版本；失败则自动恢复旧版本。

更新日志写入 `%LOCALAPPDATA%\PM Stack\updater.log`。成功更新后会保留一份上一版本；确认新版本稳定后可手动删除 `PM Stack.previous.exe`。

## 五、安全与运维建议

- 公司共享盘目录应限制为维护者可写、同事只读。
- GitHub 必须使用 HTTPS；客户端会拒绝 HTTP 地址。
- 每次发布都重新生成 `update.json`，不要手工复用旧 SHA-256。
- 先用一台测试电脑验证，再更新面向所有同事的 `update.json`。
- 当前 EXE 未做代码签名，Windows 可能显示“未知发布者”。若分发范围扩大，建议购买代码签名证书并对两个 EXE 都签名。
